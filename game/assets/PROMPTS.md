# Asset origin

Maps and object sheets were generated as clean-HD casual hidden-object illustrations (warm cream / forest / terracotta, late-afternoon light, no UI text).

Maps: 16:9 painted scenes without the findable sprites (sprites are overlaid at runtime).
Objects: 3×3 magenta `#FF00FF` prop sheets, chroma-keyed to transparent PNG.
